services:
  mariadb:
    image: bitnami/mariadb:latest
    restart: unless-stopped
    environment:
      - MARIADB_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD:-rootpassword}
      - MARIADB_DATABASE=${DB_DATABASE:-shortit}
      - TZ=${TZ:-Asia/Makassar}
    volumes:
      - mariadb_data:/bitnami/mariadb
    networks:
      - app-net

  php:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      args:
        PHP_FPM_TAG: ${PHP_FPM_TAG:-8.3-fpm}
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./:/app
      - php_fpm_sock:/var/run/php-fpm
    environment:
      - PHP_MEMORY_LIMIT=${PHP_MEMORY_LIMIT:-512M}
      - PHP_UPLOAD_MAX_FILESIZE=${PHP_UPLOAD_MAX_FILESIZE:-20M}
      - PHP_POST_MAX_SIZE=${PHP_POST_MAX_SIZE:-21M}
      - TZ=${TZ:-Asia/Makassar}
    depends_on:
      - mariadb
    healthcheck:
      test: ["CMD-SHELL", "php-fpm -t 2>/dev/null || exit 1; test -S /var/run/php-fpm/php-fpm.sock"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 20s
    networks:
      - app-net

  nginx:
    image: nginx:stable
    restart: unless-stopped
    ports:
      - "${PORT:-8000}:80"
    depends_on:
      php:
        condition: service_healthy
    volumes:
      - ./:/app
      - ./docker/nginx/laravel.conf:/etc/nginx/conf.d/default.conf:ro
      - php_fpm_sock:/var/run/php-fpm
    environment:
      - TZ=${TZ:-Asia/Makassar}
    networks:
      - app-net

volumes:
  mariadb_data:
  php_fpm_sock:

networks:
  app-net:
    driver: bridge
